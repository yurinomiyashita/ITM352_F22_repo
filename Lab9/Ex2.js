attributes  =  "Yurino;20;MIS" 
console.log(attributes.split(';',2));

/*string.split (separator.limits)
separator is optional 
(a s or regular expression to use for splitting. 
It omitted an array with the original string is returned)
*/
